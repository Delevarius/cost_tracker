// @flow

import React from 'react';
type Props = {
    
};

const TransactionTotal = React.memo((props: Props) => {
    return (
        <div>
            AA
        </div>
    );
});
export default TransactionTotal;
