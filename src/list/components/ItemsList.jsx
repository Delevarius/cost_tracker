// @flow

import React from 'react';
type Props = {
    
};

const ItemsList = React.memo((props: Props) => {
    return (
        <div>
            AA
        </div>
    );
});
export default ItemsList;
